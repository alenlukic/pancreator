{
  "schema_version": 1,
  "workspace_id": "pancreator-self-dev",
  "workspace_root": ".",
  "state_root": "runtime",
  "tracking": {
    "include": ["**/*"],
    "exclude": [
      "dist/**",
      "node_modules/**",
      "coverage/**",
      "config_overrides.json",
      "package-lock.json",
      "tsconfig.tsbuildinfo",
      "worktrees/**"
    ]
  },
  "active_config": "balanced",
  "anthropic": {
    "balanced": "claude-sonnet-5",
    "advanced": "claude-opus-5[thinking=true,context=300k,effort=high,fast=false]",
    "ultra": "claude-fable-5-1[thinking=true,context=300k,effort=high]"
  },
  "oai": {
    "balanced": "gpt-5.6-terra[context=272k,reasoning=high,fast=false]",
    "advanced": "gpt-5.6-sol[context=272k,reasoning=high,fast=false]"
  },
  "open": {
    "balanced": "glm-5.2",
    "advanced": "kimi-k3[reasoning=max]"
  },
  "cursor": {
    "balanced": "composer-2.5",
    "advanced": "grok-4.6[effort=high,fast=false]"
  },
  "defaults": {
    "orchestrator": "",
    "meta-orchestrator": "",
    "planner": "",
    "coder": "",
    "metacritic": "",
    "reviewer": "",
    "verifier": "",
    "remediator": "",
    "remediator-severe": "",
    "shepherd-reviewer": "",
    "qa-tester": "",
    "designer": "",
    "design-reviewer": "",
    "design-qa": "",
    "release-steward": "",
    "librarian": "",
    "decomposer": "",
    "investigator": "",
    "spotfixer": "",
    "harness-technician": "",
    "repo-technician": "",
    "harness-workflow-qa": "",
    "hypervisor": ""
  },
  "$operator": {
    "summary": "Named model configurations. Each configuration maps every checked-in Cursor persona to the model that should execute it.",
    "note": "This file carries the recommended defaults a release can update. Put per-checkout preferences (active_config, model overrides) in the untracked config_overrides.json, which merges over this file. After changing either, run ./.pancreator/bin/pan models --sync. New runs snapshot the active mapping so later config edits do not change an in-flight run."
  },
  "configs": {
    "balanced": {
      "summary": "Balance of focused coding, strong review, and frontier planning.",
      "coder": "",
      "remediator": "",
      "reviewer": "",
      "verifier": "",
      "qa-tester": ""
    },
    "advanced": {
      "summary": "Higher-reasoning coding and QA for complex or high-risk work.",
      "coder": "",
      "remediator": "",
      "reviewer": "",
      "verifier": "",
      "qa-tester": ""
    },
    "ultra": {
      "summary": "Extreme configuration for the most demanding tasks.",
      "planner": "",
      "coder": "",
      "remediator": "",
      "remediator-severe": "",
      "reviewer": "",
      "verifier": "",
      "shepherd-reviewer": "",
      "qa-tester": ""
    },
    "auto": {
      "summary": "Cursor-managed routing except for the bounded coding workhorse.",
      "planner": "",
      "decomposer": "",
      "reviewer": "",
      "verifier": "",
      "qa-tester": ""
    }
  },
  "operator_involvement": {
    "$operator": {
      "summary": "Named profiles controlling how heavily the operator gates each workflow stage. Select one per run with ./bin/pan init --involvement <profile>; omitting the flag uses active.",
      "note": "gates maps a stage slug (or * for every stage) to the gate that stage uses for the run. contracts names run-wide contracts the run abides by. Runs snapshot the resolved profile, so later edits here do not change an in-flight run. Inspect available profiles with ./bin/pan involvement."
    },
    "active": "standard",
    "profiles": {
      "standard": {
        "summary": "Workflow-declared gates, unchanged. The operator ratifies the plan and approves release preparation."
      },
      "hands-off": {
        "summary": "Minimum interruption. The supervisor ratifies the plan instead of the operator; release preparation still stops for explicit operator approval.",
        "gates": {
          "plan": "supervisor"
        }
      },
      "technical-director": {
        "summary": "The operator refines the technical plan with its author before implementation and responds to the independent review before the run continues.",
        "contracts": ["technical_director"]
      },
      "high-touch": {
        "summary": "Every stage stops for explicit operator approval, and the technical-director refinement contract is active.",
        "gates": {
          "*": "operator"
        },
        "contracts": ["technical_director"]
      }
    }
  },
  "verification": {
    "$operator": {
      "summary": "Named verification levels controlling which repository-check profile each shell gate runs. Select one per run with ./bin/pan init --verification <level>; omitting the flag uses active.",
      "note": "Built-in levels: minimal (static and fast gate the implement and remediate loops, no submission gate runs full), light (the verify submission gate runs the full profile once on a passing verdict, and the remediate submission gate runs it once when the repair is ready to ship; the returning verify gate accepts that recorded pass at an unchanged fingerprint), thorough (alias of light kept for existing snapshots). Agents iterate on blast-radius tests, run the fast profile at most once each as validation, and never run full. The full profile is never baselined before implementation. Operator-declared levels here merge over the built-ins. Runs snapshot the resolved level. Change an in-flight run with ./bin/pan verification <run-id> set <level>. Inspect levels with ./bin/pan verification."
    },
    "active": "light"
  },
  "away_mode": {
    "enabled": false
  },
  "installation_mode": "self_development"
}
