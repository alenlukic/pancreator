/**
 * @packageDocumentation Phase 0a scaffold. Shared types and logic land in later phases.
 * This package MUST NOT depend on other `@tesseract/*` packages.
 */
export const TESSERACT_CORE_VERSION = "0.0.0" as const;
