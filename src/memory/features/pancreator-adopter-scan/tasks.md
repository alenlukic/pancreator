# Task List - @pancreator/adopter-scan

- [x] T1: Export `scanRepository(rootPath)` and `AdoptionScanReport` (languages, workspace tooling, GitHub Actions presence, vitest/jest from `package.json` deps).
- [x] T2: README Quickstart with `pnpm` build/test and sample import (`pancreator.adopter_scan.readme_ergonomics`).
- [x] T3: Vitest fixtures for Node/vitest detection and `.github/workflows` presence.
- [x] T4: `typecheck`, `test`, `attw`, `publint` scripts.
- [ ] T5 (deferred): Deeper semantic classification, SME proposals, `--exhaustive` low-confidence path (PRD Q15); integration with `pan init` adopt flow (Phase 4).
