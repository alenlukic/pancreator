# Task List - @pancreator/contract

- [x] T1: Confirm package scaffold and exported surface satisfy `pancreator.contract.package_shape` (package.json, README.md, src/index.ts; Phase 3 slice adds wrapper types and tests).
- [x] T2: Confirm README Quickstart satisfies `pancreator.contract.readme_ergonomics` (## Quickstart with five `pnpm` commands; llm-judge not re-run in this slice).
- [x] T3: Run package conformance checks and capture failures (build, typecheck, test, attw, publint per Phase 3 verification).
- [x] T4: Resolve contract failures with minimal, scoped package edits.
- [x] T5: Re-run contract checks and record green status for Phase 2 completion (Phase 3 step 2 spine delivered; `readme_ergonomics` remains a manual or scheduled llm-judge run).
