# daedaline (meta)

Unscoped meta package to align M1 `workspace` dependencies and later surface re-exports. Phase 0a is intentionally thin: the package lists every scoped `@daedaline/*` member as a `workspace` dependency. Full re-exports and the `ddl` entry story land in Phase 3+.
