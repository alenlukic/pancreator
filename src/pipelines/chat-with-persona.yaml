id: chat-with-persona
version: "1"
metadata:
  daedaline-bootstrap-only: true
  daedaline-m1-deferred: true
  description: >-
    PRD §11 pipeline skeleton only — not on the M1 execution path. Conversation
    Mode (US-2) runtime, `ddl chat`, and Persona Ensemble wiring are backlog-deferred
    to M2+. The bootstrap registers this YAML so `@daedaline/pipeline` can load and
    compile the shape; operators MUST NOT treat M1 as blocked on executing this pipeline.
  references:
    - kind: lines
      path: docs/PRD.md
      range: [1, 80]
      contentHash: 2ce8e5c
      note: Cited for product context; pipeline structure derives from PRD §11.
stages:
  - id: dispatch
    label: Dispatch the message to the named persona
