id: tesseract.contract_runner_rego.package_shape
kind: rego
severity: block
applies_to:
  kind: file-path
  glob: "packages/@tesseract/contract-runner-rego/**"
owner: contract-writer
description: |
  When the Phase 2 scaffold for `packages/@tesseract/contract-runner-rego` is evaluated, the contract-runner SHALL report a block-level failure unless `package.json`, `README.md`, and `src/index.ts` exist in that package directory.
references:
  - kind: lines
    path: BOOTSTRAP.md
    range: [122, 159]
    contentHash: TBD-on-commit
    note: "Bootstrap Phase 2 package contract requirement and dependency order."
  - kind: lines
    path: PRD.md
    range: [1116, 1126]
    contentHash: TBD-on-commit
    note: "M1 substrate package scope and interfaces."
spec: /memory/features/tesseract-contract-runner-rego/contracts/tesseract.contract_runner_rego.package_shape.rego
runtime:
  package: tesseract.phase2.contract_runner_rego
  query: data.tesseract.phase2.contract_runner_rego.deny
metadata:
  tesseract.contract_id: tesseract.contract_runner_rego.package_shape
  tesseract.applies_to: file-path:packages/@tesseract/contract-runner-rego/**
