schema_ref: "../schemas/latest.yaml"
id: "high-remediation-blocking"
severity: "high"
trigger_modes:
  - operator-on-demand
  - structure-change
scope:
  surface: "review-and-approval-gates"
  artifacts:
    - "work/<id>/review.md"
    - "inbox/out/"
assertion:
  statement: "High findings block review_passes and human_approval."
  pass_criteria: "Run output records zero open high findings after remediation rerun."
