id: tesseract.memory.package_shape
kind: rego
severity: block
applies_to:
  kind: file-path
  glob: "packages/@tesseract/memory/**"
owner: contract-writer
description: |
  When the Phase 2 scaffold for `packages/@tesseract/memory` is evaluated, the contract-runner SHALL report a block-level failure unless `package.json`, `README.md`, and `src/index.ts` exist in that package directory.
references:
  - kind: lines
    path: BOOTSTRAP.md
    range: [122, 159]
    contentHash: TBD-on-commit
    note: "Bootstrap Phase 2 package contract requirement and dependency order."
  - kind: lines
    path: PRD.md
    range: [1116, 1126]
    contentHash: TBD-on-commit
    note: "M1 substrate package scope and interfaces."
spec: /memory/features/tesseract-memory/contracts/tesseract.memory.package_shape.rego
runtime:
  package: tesseract.phase2.memory
  query: data.tesseract.phase2.memory.deny
metadata:
  tesseract.contract_id: tesseract.memory.package_shape
  tesseract.applies_to: file-path:packages/@tesseract/memory/**
