Bring the ad-hoc changes of this session to a mergeable state, and stop at the integration boundary. `$ARGUMENTS` is an optional scope hint; when it carries `--worktree <name>`, forward that selection to the card step and treat the remainder as the hint.

1. Read `{{PANCREATOR_HARNESS_PATH}}AGENTS.md`.
2. Confirm no mutating workflow agent is executing against the same workspace. If one is, stop and tell the operator to terminate it before retrying.
3. Run `{{PANCREATOR_PAN_COMMAND}} governance card --mode harden --worktree <name>` and read the card it writes in full. Omit `--worktree <name>` when the operator names no worktree; when present, the card creates or resolves the worktree and binds the session workspace to it. Do not assemble policy text by hand, and do not generate a second card for the same session.
4. Read `{{PANCREATOR_HARNESS_PATH}}library/skills/harden.md` and follow its procedure in order: assess the extent of the changes, exercise the changed functionality, review and analyze through exactly one `pan-reviewer` over a single capture, inspect a rendered surface when one changed, close the ranked gaps, and prepare for integration without performing it. `DELEGATE-001` on the card owns the delegation; the review is standalone, and the reviewer MUST NOT edit, stage, or commit. When the findings warrant more, name `/pan-review` as the deeper option rather than running it.
5. Do not commit, push, merge, rebase, publish, deploy, or change Git history. Prepare the branch and name the operator's integration command without running it.
6. Surface the outcome in the chat inside one fenced `markdown` block so it is directly copyable: the resolved scope, the checks run with their real results, the review findings and their disposition, the visual-inspection evidence or the environment block, and the named integration step the session did not run.
