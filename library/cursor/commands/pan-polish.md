Bring the UI and design changes of this session into conformance with the design handbook and the design system that owns each touched surface. An empty `$ARGUMENTS` selects the working tree's UI and design changes; a non-empty `$ARGUMENTS` is the design task itself. When `$ARGUMENTS` carries `--worktree <name>`, forward that selection to the card step and treat the remainder as the invocation shape above.

1. Read `{{PANCREATOR_HARNESS_PATH}}AGENTS.md`.
2. Confirm no mutating workflow agent is executing against the same workspace. If one is, stop and tell the operator to terminate it before retrying.
3. Run `{{PANCREATOR_PAN_COMMAND}} governance card --mode polish --worktree <name>` and read the card it writes in full. Omit `--worktree <name>` when the operator names no worktree; when present, the card creates or resolves the worktree and binds the session workspace to it. Do not assemble policy text by hand, and do not generate a second card for the same session.
4. Read `{{PANCREATOR_HARNESS_PATH}}library/skills/polish.md` and follow its procedure: resolve the scope, resolve the design system by surface, conform through the capture-score-fix loop, enforce the design system where one is declared, and ask the operator before generating a design system for a surface that has none. `DESIGN-001` on the card owns the design handbook reference.
5. Edit only the UI and design surface of the resolved scope. Do not commit, push, merge, publish, deploy, or change Git history.
6. Surface the outcome in the chat inside one fenced `markdown` block so it is directly copyable: the resolved scope, the design system resolved per surface or the approval the session asked for, the heuristics scored, the changes made, the before-and-after evidence paths, and any residual non-conformance.
