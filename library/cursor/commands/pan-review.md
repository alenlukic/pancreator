Run one review-squad pass over the target named by `$ARGUMENTS`.

You coordinate the session in this conversation. There is no workflow run, no stage contract, and no gate. This command produces findings and a verdict; it authorizes no edit, no commit, and no push.

1. Read `{{PANCREATOR_HARNESS_PATH}}AGENTS.md`; preserve `$ARGUMENTS` verbatim in a uniquely named file under `{{PANCREATOR_HARNESS_PATH}}runtime/inbox/` as the review input. When `$ARGUMENTS` is empty, write the target you derive in step 3 into that file instead.
2. Run `{{PANCREATOR_PAN_COMMAND}} governance card --mode review --request <harness-relative-input>` and read the card it writes. It resolves the complete review governance, including `REVIEW-001` and its reference to the squad procedure. Read that reference. Do not assemble policy text by hand.
3. Resolve exactly one target. A ref range (`main...my-branch`), a single ref (compared against its merge base with the repository's default branch), or a pull request number or URL are valid. For a pull request, run `gh pr view <n> --json baseRefName,headRefName,headRefOid` to resolve its base branch and head commit. Then run `git fetch origin pull/<n>/head` and treat `<base-branch>...FETCH_HEAD` as the range. An explicit path set narrows a resolved range rather than replacing it: resolve the range first, restrict the capture to those paths, and say so; the scope check in step 6 still runs on the whole head. With no argument, use the current branch against its merge base with the default branch. State the resolved range before you capture it. Ask the operator when the request is ambiguous rather than absent.
4. Capture the target once to `review-target.diff` beside the card, using `git diff --merge-base` for a single ref or `git diff <base>...<head>` for a range. Confirm the capture is non-empty; an empty capture ends the session with that fact and no review.
5. Bind the workspace to the target's head before you delegate. The command reviews any ref from any checkout, but the reviewing agents read code to confirm what the diff only claims, so the tree they read MUST be the tree the diff applies to. Compare `git rev-parse HEAD` against the target head. When they differ, run `{{PANCREATOR_PAN_COMMAND}} worktree create <name> --from <target-head> --json`, or resolve an existing worktree already at that commit, and use the reported path as the review workspace. Name that path to the operator and to the coordinator. When the operator named a worktree on the command, add `--worktree <name>` in step 2 so the card binds the session to it, and confirm it sits at the target head.
6. Run `{{PANCREATOR_PAN_COMMAND}} governance review-scope --target <target-head> --base <base> --json` from the review workspace path you bound in step 5. `<base>` is the base you captured in step 4. Pass it every time. The check reads the review closure from the working tree, and it fails with `REVIEW_CLOSURE_REVISION_MISMATCH` when that tree is not at the target head. Confirm that `closure_revision` in the output equals the target head. Read `conflicts` by tier and `standards_delta`. **Instrument** conflicts (lineup, charters, coordinator, mode policy, entry points, the scope check, the reviewer model mapping): exclude those paths from the squad's verdict and delegate one `pan-reviewer` subagent over just those paths for an independent read. Tell it that this is a standalone review, that it MUST NOT edit, stage, or commit anything, and that its findings file is its only write; its persona's bounded remediation does not apply here. **Conduct** conflicts (a policy on your own card or guidance it delivers): rebuild your card with `{{PANCREATOR_PAN_COMMAND}} governance card --mode review --request <input> --base <base> --target <target-head> --out <the card path from step 2>`, adding `--worktree <name>` when you passed one in step 2, and read it again; the base-conduct card replaces the first one beside the capture, marks each such policy's head text as under review, and renders its base text, which your conduct follows. **Substrate** conflicts (validation code, test helpers, check wrappers, gate cache, exemption registries, changelog): keep them in scope, and tell the coordinator that any verification leaning on them is tainted. `clean: true` means no conflict at any tier. Carry `standards_delta` into the report verbatim: a rule that differs from its base is a decision for the operator, not a finding.
7. Gather the intent sources — the commit subjects in the range, the request file, and any linked card or acceptance criteria the operator named — as paths and refs. Do not write the brief yourself: the coordinator derives it in resolve mode, so the party that decides what counts as "already answered" is not the party that may have authored the change. If this conversation authored any part of the target, say so now; it goes into the report as a disclosed deviation, and a fresh session is the better reviewer.
8. Delegate exactly one `pan-shepherd-reviewer` subagent in **resolve** mode with the captured diff path, the review workspace path, the intent sources, the resolved target, the tiered conflicts, and the standards delta. It returns the intent brief it derived, the lineup, each charter verbatim, the calibration bar and the boundaries from the squad procedure, the finding shape, the exclusions, and the tainted paths, and edits nothing. Its model comes from the `shepherd-reviewer` mapping in `config.json`.
9. Issue the dimension fan-out yourself, at the top level and in one message: one ad-hoc subagent per charter the coordinator returned, each carrying the capture path, the workspace path, the derived intent brief, its charter verbatim, the calibration bar and the boundaries from the squad procedure, the finding shape, the exclusions, and the tainted paths. State in each prompt that the dimension agent MUST NOT edit a file, stage, commit, push, or write workflow state. Dispatch each dimension agent in the background and run the `DELEGATE-001` cadence check until every agent reaches a terminal state. On a repeat review, withhold the prior finding set from the dimension agents; they review the delta blind, and the coordinator reconciles in join mode. Omit `model` so each inherits this session's model. A dimension agent spawned by the coordinator would be a nested spawn and run on the platform default model, which is why the fan-out is yours. Collect each agent's raw findings without judging them.
10. Delegate the same `pan-shepherd-reviewer` again in **join** mode with the raw finding files, the capture path, the review workspace path, the derived intent brief, each charter whose subagent could not start, and, on a repeat review, the prior finding set. It applies each undelivered charter itself before it joins, then merges duplicates, drops what the intent brief answers with the brief line quoted for each drop, marks tainted verification, ranks, and returns the finding set with the standards delta and a pass or fail verdict. Do not join, rank, drop, or grade a finding yourself.
11. Do not edit, stage, commit, push, or write workflow state at any point, and do not act on a finding. When the operator wants a finding fixed, that is a separate `/pan-spotfix` or a systematic run.
12. Surface the coordinator's complete report in the chat inside one fenced `markdown` block so it is directly copyable. Include the resolved target, the capture path, the review workspace path, the conflicts by tier with what the independent reviewer said about the instrument tier, the standards delta, any authorship disclosure, the dimensions that ran, the dimensions that returned nothing, and two verdicts with one combined outcome. The coordinator verdict covers the graded tiers. The independent reviewer verdict covers the instrument tier, or reads not applicable when no instrument conflict exists. The combined outcome is fail when either verdict is fail. Do not restate the independent verdict as a graded finding. Do not rewrite or summarize the report outside the block.
