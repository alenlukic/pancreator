{
  "id": "BRIEF-001",
  "title": "Operator brief structure and presentation",
  "severity": "hard",
  "summary": "Requested and generated workflow-stage narratives MUST use the shared brief contract and render to self-contained semantic HTML.",
  "instructions": [
    "A workflow invocation MUST include a brief contract only when the operator requests workflow artifacts.",
    "A requested workflow-stage narrative MUST use schema-valid brief data at the exact source path that the harness creates.",
    "A worker MUST edit the declared source in place and MUST NOT run the renderer.",
    "The harness MUST render requested HTML during submission.",
    "A generated workflow-stage narrative MUST use the retained brief system and the latest submitted stage records.",
    "The rendered HTML MUST be artifact 0 when an invocation requests a brief.",
    "The transient source JSON MUST NOT be a final stage artifact.",
    "The harness MUST delete transient source JSON after successful rendering and validation. It MUST retain the source after a failure.",
    "Path drift, invalid brief data, or rendering defects are governance diagnostics for ship review. They MUST NOT loop the workflow to implementation.",
    "A brief MUST begin with an executive-summary section that states the bottom line first: what changed or matters, why it matters, and outcomes or required actions when applicable.",
    "Brief data MUST describe content and semantic intent; layout, typography, color, spacing, and responsive behavior MUST remain in the shared or project design system.",
    "Section headings MUST use concise titles and a registered semantic key. Renderers MUST resolve the heading emoji from that registry rather than accepting ad hoc emoji choices.",
    "Within one repository, each registered section emoji MUST retain one meaning. Project definitions MUST extend rather than override Pancreator-owned semantic keys.",
    "Cards SHOULD separate metadata from substantive detail through field placement, SHOULD expose clear actions when action is possible, and MUST avoid unnecessary nesting, decorative clutter, and raw-log substitution.",
    "Standalone modes MUST retain their declared artifact behavior.",
    "Canonical control records, source documentation, direct-use PR copy, machine records, and historical artifacts MAY remain Markdown or JSON."
  ]
}
