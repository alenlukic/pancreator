# Operator section
- 👀 **In this file:** Pipeline definition `init-greenfield`.
- ⚖️ **Why it matters:** Quick orientation for init greenfield before agents load the full contract.
- 🧭 **See also:**
  - pancreator/lib/pipelines/README.md
  - pancreator/lib/memory/handbook/pipeline-state-contract.md
id: init-greenfield
version: "1"
metadata:
  pancreator-bootstrap-only: false
  description: >-
    PRD §11 MVP pipeline. Greenfield repo bootstrap: scaffold the AGENTS.md +
    handbook seeds + persona roster, draft the inaugural feature spec, and
    author the first plan. Adopter scan is omitted (no prior state).
  references:
    - kind: lines
      path: .docs/PRD.md
      range: [1, 80]
      contentHash: 2eb6aa4
      note: Cited for product context; pipeline structure derives from PRD §11.
stages:
  - id: scaffold
    label: Emit AGENTS.md, handbook seeds, persona roster, and skill packs
  - id: spec
    persona: intake-analyst
    label: Author the inaugural Engineering Spec from the seed directive
  - id: plan
    persona: tech-lead
    label: Author the first plan, ADR, and touch-set
