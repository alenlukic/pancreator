persona: persona-designer
description: When a human invokes the persona-designer during bootstrap Phase 1 or when the Librarian proposes a new SME (M4+), the `persona-designer` SHALL author a conforming Pancreator subagent persona specification to the Anthropic Claude Agent SDK 16-field YAML frontmatter spec and matching tool-agnostic rule specs under `lib/personas/rules/`.
globs:
  - lib/personas/rules/*.yaml
  - lib/personas/**/*.md
  - .cursor/agents/**/*.md
alwaysApply: false
