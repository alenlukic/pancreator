# Cursor semantic indexing policy for this repository.
# Operators SHOULD restart or reindex Cursor after changes.
# Explicit @-mentions and intentional reads still reach excluded paths.

# Active and archived run artifacts
.pan/work/**
.pan/archive/work/**
lib/inbox/**
.pan/archive/inbox/**

# Internal product and self-development surfaces (entire .docs/ tree, including .docs/README.md)
.docs/**
.docs/README.md
AGENTS.md
lib/memory/adr/**
lib/memory/backlog/**
lib/memory/research/**

# Generated manifests / bulky machine outputs
**/migration-manifest*.json
**/*.dry-run.json
**/*.post-write.json
**/*.write.json

# Generated or low-signal durable indexes/reports
lib/memory/**/*.json
lib/memory/**/delivery-report.md
lib/memory/**/index.json

# Lockfile rarely needed for semantic repo reasoning
pnpm-lock.yaml

# Internal operating content: narrow default semantic sweeps; route reads via handbook index and personas when needed.
lib/memory/handbook/**
lib/personas/**
lib/personas/skills/**
.cursor/rules/**

# Cursor-native mirrors are runtime surfaces, not semantic source of truth.
# OPERATOR MUST confirm Cursor still discovers every custom agent under
# .cursor/agents/ before merging any change that keeps this exclusion.
.cursor/agents/**
