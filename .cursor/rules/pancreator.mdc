---
description: Pancreator v2 workflow control rules
alwaysApply: true
---
The terms MUST, MUST NOT, SHOULD, SHOULD NOT, and MAY use RFC 2119 meanings.

You MUST read `AGENTS.md` before operating a Pancreator run. You MUST use `./bin/pan` and MUST NOT hand-edit runtime state. You MUST read the active invocation card before expanding context. Operator approvals and irreversible actions MUST remain operator-owned. TypeScript changes MUST conform to `governance/handbooks/typescript/style-guide.md`.

For systematic workflow edits, agents MUST use the `pan changes begin|commit|cancel` protocol and MUST NOT directly edit lock, ledger, index, or generated run-state files. An operator-selected `/pan-spotfix` MAY edit tracked files directly only under `library/skills/spotfix.md` and only when no mutating workflow agent is executing against the same workspace. While mutating workflows run, external edits SHOULD be avoided because Pancreator locks are cooperative only.

`./bin/pan set-stage` is operator-only. Agents MUST NOT invoke it or delegate its invocation.

`rtk` (https://github.com/rtk-ai/rtk) globally wraps Cursor shell commands. Agents MAY see summarized or truncated output and SHOULD rerun with explicit bounded capture when exact shell bytes matter.

`workdesk/` is a separate, private git repository nested in this checkout and is gitignored by Pancreator. You MUST NOT track or commit `workdesk/` contents from the Pancreator repository. When working inside `workdesk/`, you MUST follow `workdesk/AGENTS.md` and the relevant subproject's `AGENTS.md`.
