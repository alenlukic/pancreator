---
description: When the `feature-delivery` pipeline reaches the `plan` stage with a ratified Engineering Spec at `/src/memory/features/<id>/spec.md`, the `tech-lead` SHALL emit `/src/work/<day>/<id>/plan.md`, `/src/work/<day>/<id>/adr-draft.md`, and `/src/work/<day>/<id>/touch-set.json` for the downstream `implement` stage.
globs:
  - "src/memory/adr/*.md"
  - "src/work/**/adr-draft.md"
  - "src/work/**/plan.md"
  - "src/work/**/touch-set.json"
alwaysApply: false
---

@src/personas/tech-lead.md
