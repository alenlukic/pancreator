{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "attw": {
      "dependsOn": ["build"]
    },
    "publint": {
      "dependsOn": ["build"]
    }
  }
}
